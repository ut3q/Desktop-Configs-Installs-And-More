return {
	windowGapsIn = 3, -- Gaps between multiple windows
	windowGapsOut = 6, -- Outer gaps when multiple apps are open
	singleWindowGapsOut = 6, -- Forces single windows to use the exact same outer gap!
}
